import React from "react";
import ReactDOM from "react-dom/client";
import DroneQuiz from "./App";  // 반드시 이 이름!

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DroneQuiz />
  </React.StrictMode>
);
